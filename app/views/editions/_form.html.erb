<%= form_for [@art_piece, @production, @edition] do |f| %>
  <% if @edition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edition.errors.count, "error") %> prohibited this production from being saved:</h2>

      <ul>
      <% @edition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">

    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="form-group">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="form-group">
    <%= f.label :purchaser %><br />
    <%= f.text_field :purchaser %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>